# LLM Configuration
# Options: "anthropic" (requires API key) or "mock" (for demo without API)
LLM_PROVIDER=mock

# Anthropic API Key (only needed if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your_api_key_here

# Server Configuration
PORT=3000
NODE_ENV=development

# Logging
LOG_LEVEL=info
